---
title: Post title goes here
description: One sentence that shows up in search results, the card, and the OG image.
date: 2026-09-05
tags: [Rails, Kubernetes]
draft: true
---

Opening paragraph. Drafts (`draft: true`) render in `next dev` and are never
published by a production build, so this file is a safe scratchpad.

## A section heading

Body copy, with `inline code`, a [link](https://bon.so), and **emphasis**.

```ruby
class TrustAI::Prompt
  def initialize(template) = @template = template
end
```

> A pull quote, for when a line deserves the room.

- List item
- Another one
